<div id="form-container" class="container">
  <%= simple_form_for(@category) do |f| %>
    <div id="info">
      <%= f.input :name, placeholder: "starter" %>
      <%= f.input :icon %>
      <%= f.submit "Submit", class: "btn-large nav-link" %>
    </div>
  <% end %>
</div>
